<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/styleprofile.css">
</head>
<body>
    <header>
        <h1>PixiePost</h1>
    </header>
    <h3>Welcome : @<%=username%></h3>
    <div class="posts">
        <%data.forEach(post => {%> 
            <div class="post">
                <img src="<%=post.image%>" class="img">
                <p class="cap"><%=post.caption%></p>
                <a href="http://localhost:8000/profile/<%=username%>/post/<%=post.id%>/edit" class="edit btn">Edit post</a>
                <form method="post" action="/profile/<%=username%>/post/<%=post.id%>?_method=DELETE">
                    <button class="del btn">Delete Post</button>
                </form>
            </div>
        <%})%>
    </div>
    <br>
    <a href="http://localhost:8000/profile/<%=username%>/post/new" class="edit btn">Add new post</a>
</body>
</html>